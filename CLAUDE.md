# Toolbox

This is a monorepo of independent, self-contained projects. Each subdirectory is its own project with its own dependencies, language, and conventions.

When working on a project, scope all exploration and changes to that project's directory. Do not search across or modify other projects.
